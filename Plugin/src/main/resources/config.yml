# GitHub repo hosting arena packs, fetched via raw.githubusercontent.com (no auth)
repo:
  # owner/name of the GitHub repository
  slug: "MetallicGoat/MBedwarsArenaPacks"
  branch: "main"
  # Path of the pack index inside the repository. Pack directories listed in it
  # are resolved relative to this file's folder, so the default looks for
  # Packs/index.json and finds each pack at Packs/<name>/
  index-path: "Packs/index.json"

network:
  connect-timeout-ms: 10000
  read-timeout-ms: 30000
  # How long a fetched index is served from cache before re-downloading
  index-cache-seconds: 300

import:
  # Folder name for imported worlds. {arena} is replaced with the
  # lowercased arena name. Import fails if the folder already exists.
  world-name-format: "arenapacks_{arena}"

export:
  # Only export dealer/hologram NPCs located inside the arena region.
  # Disable if your holograms sit slightly outside the region corners.
  only-holograms-inside-region: true
